__pycache__
.mypy_cache
.DS_Store
# Local secrets (OPENAI_API_KEY etc.) — never commit; .env.example is the template.
.env
.env.local
anki.prof
target
/user.bazelrc
.dmypy.json
/.idea/
/.vscode
/.bazel
/windows.bazelrc
/out
node_modules
.n2_db
.ninja_log
.ninja_deps
/extra
yarn-error.log
ts/.svelte-kit
.yarn
.claude/settings.local.json
.claude/user.md
.venv
docs/_build/
docs/autoapi/
.complexipy_cache

# Copyrighted MCAT source material — private ground truth only, never commit/distribute.
# (Typo fixed: was "groud_truth". Covers the PDFs and any locally-curated eval
# sets/results under resources/ground_truth/.)
resources/ground_truth/*
